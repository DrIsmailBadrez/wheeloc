<%= @offer.title %>
<%= @offer.description %>
<%= @offer.address %>
<%= @offer.price %>

<%= link_to "Delete",
    offer_path(@offer),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>

<%= link_to "Edit this offer", edit_offer(@offer) %>
